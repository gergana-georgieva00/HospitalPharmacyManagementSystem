@using HospitalPharmacyManagementSystem.Services.Data.Interfaces
@using HospitalPharmacyManagementSystem.Services.Data;
@using HospitalPharmacyManagementSystem.Web.Infrastructure.Extentions
@using HospitalPharmacyManagementSytem.Web.Infrastucture.Extentions;
@model DrugAllViewModel

@*@inject IPharmacistService AgentService
@inject IDrugService HouseService
@{
    bool isUserAgent = await PharmacistService.PharmacistExistsByUserIdAsync(User.GetId()!);
    bool isHouseRentedByUser = await DrugService.IsRentedByUserWithIdAsync(Model.Id, User.GetId()!);
}*@

<div class="col-md-4">
    <div class="card mb-3">
        <img class="card-img-top" src="@Model.ImageUrl" alt="Drug Image">
        <div class="card-body text-center">
            <h4>@Model.BrandName</h4>
            <h6>
                Price:
                <b>@($"{Model.Price:f2}") BGN</b>
            </h6>
            <br />
            <a asp-controller="Drug" asp-action="Details" asp-route-id="@Model.Id"
               class="btn btn-success">Details</a>

            @if (User.Identity?.IsAuthenticated ?? false)
            {
               @* @if ((isUserAgent && isAgentOwnerOfTheHouse) || User.IsAdmin())
                {*@
                    <a asp-controller="Drug" asp-action="Edit" asp-route-id="@Model.Id"
                       class="btn btn-warning">Edit</a>
                    <a asp-controller="Drug" asp-action="Delete" asp-route-id="@Model.Id"
                       class="btn btn-danger">Delete</a>
                //}
                <p></p>

               @* @if (!Model.IsRented)
                {
                    @if (!isUserAgent || User.IsAdmin())
                    {
                        <form class="input-group-sm" asp-controller="House"
                  asp-action="Rent" asp-route-id="@Model.Id" method="post">
                            <input class="btn btn-primary" type="submit" value="Rent" />
                        </form>
                    }
                }
                else
                {
                    @if (!isUserAgent && isHouseRentedByUser)
                    {
                        <form asp-controller="House" asp-action="Leave"
                  asp-route-id="@Model.Id" method="post">
                            <input class="btn btn-primary" type="submit" value="Leave" />
                        </form>
                    }
                }*@
            }
        </div>
    </div>
</div>